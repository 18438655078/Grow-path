一、算法(algorithms)
        1、排序算法
        2、影响算法的好坏的因素：
            a)将被排序的项数；            (要排序的数组大小)
            b)这些项已被稍微排序的程度；   (数组的顺序乱不乱)
            c)对于每一项值的可能限制；     (有没有限制的取值范围，限制范围的大小)
            d)计算机的体系结构。          (电脑运行快不快)
        3、算法共有特征：
            a)存在许多候选解；      (一个问题可以用很多种算法来解决，但是最优的或者真正的解很难有，想找到是一个很大的挑战)
            b)存在实际应用。        (大部分算法都会在实际生活中应用，不然算法有什么意义呢？)
        4、答案网址：https://ita.skanev.com
        
